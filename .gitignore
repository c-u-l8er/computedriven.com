# The pre-redesign homepage, kept on disk as the only copy of a page that was
# never under version control. Not published: a superseded page served at a real
# URL is exactly the kind of thing the rest of this site argues against.
index.prev.html
